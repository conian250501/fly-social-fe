"use client";
import ModalLogout from "./components/ModalLogout";

type Props = {};

const Page = (props: Props) => {
  return (
    <section>
      <ModalLogout />
    </section>
  );
};

export default Page;
