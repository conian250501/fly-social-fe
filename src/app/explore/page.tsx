import React from "react";
import styles from "./page.module.scss";
type Props = {};

const Page = (props: Props) => {
  return <div className={styles.explorePage}>Page</div>;
};

export default Page;
