"use client";
import SettingLinks from "@/components/SettingLinks";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <SettingLinks />
    </div>
  );
};

export default Page;
