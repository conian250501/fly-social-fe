import React from "react";
import styles from "./userList.module.scss";
type Props = {};

const UserList = (props: Props) => {
  return <div className={styles.userListWrapper}>UserList</div>;
};

export default UserList;
