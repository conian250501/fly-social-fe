import React from "react";
import styles from "./loadingDots.module.scss";
type Props = {};

const LoadingDots = (props: Props) => {
  return <div className={styles.loadingDots}></div>;
};

export default LoadingDots;
